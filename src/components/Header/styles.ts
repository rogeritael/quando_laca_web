import styled from "styled-components";

export const HeaderComponent = styled.header`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 24px;
    width: 100%;
    padding: 24px;
`;